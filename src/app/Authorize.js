'use strict';

const _ = require('lodash');

class Authorize {
  constructor(env) {
    this.env = env;
  }

  init(event) {
    if (event.httpMethod === 'POST') {

    } else {

    }
  }

  callback(event) {

  }
}